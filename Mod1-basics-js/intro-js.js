// Introduccion a Javascript - Alchemy Smart Contracts 
// Nombre: Brayan Ronaldo 
// Codigo-ide: vscode
// Fecha : 2025-07-17

// First config file
// Declaracion de variables constantes 
// VARIABLES - CONSTANTES
// se realiza una declaracion de una variable de nombre a con un valor el cual se termina con un centencia " ; "
// se reconoce "=" como un valor de asignacion, el cual almacena un valor 
// se realiza la declaracion de tokens 
/*
    Utilizando la sintaxis de JavaScript, el compilador asigna un conjunto de reglas para determinar el significado de los tokens anteriores.
*/

// al momento de  interpretar el codigo el compilador reconoce linea por linea, 
const a1 = 4;


// Declaracion de multiples variables 
const a = 4; // asignacion de variable a
const b = a;  // b = a ahora b contiene b

//console.log(a);

// Booleanos varibles de estado verdadero o falso 

// para estas declaraciones se puede de utilizar sintaxis no mayuscula 
// en javascript se utiliza la sintaxis camelCase para declarar variables
const loggingIn = true;
const purchasedInloggin = false;

const a = true;
const b = false; 
// export.modules = {a,b}

// Texto o cadenas Strings 
const dname = "Javascript"; 
const myName = "Brayan Ronaldo";
const LastName = "Sanchez";
const nicknm = "0xZron";


// mensajes combinados 
// declaraciones de variables con un tipos de comillas distintas 
// para realizar este tipo de declaraciones se utiliza comillas invertidas 
/*
 o backticks el cual es interpolar y agregar variables dentro de una cadena de texto en una misma variable;

*/

const messagename = `Hola ${myName} bienvenido a el analisis con javascript`;
const msgConv = `hola ${dname} my name es ${myName} ${LastName} `;

// inmutabilidad en las declaraciones de las variables asignadas 

// se declara con let la variable a, la cual puede ser reasignada en el transcurso del codigo
// no constante 
let a = 3;
a = 6;

// fin del contenido 
// tambien se puede de definir con 
// var es una variable global, la cual puede ser reasignada en el transcurso del codigo
var a = t ; 
a = c; 


